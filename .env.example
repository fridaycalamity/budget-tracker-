# Environment Variables Example
# Copy this file to .env.local for local development
# These variables are optional for the Budget Tracker application

# Vite Base URL (for subdirectory deployments like GitHub Pages)
# Default: '/' (root)
# Example for GitHub Pages: '/repository-name/'
# VITE_BASE_URL=/

# Node Environment
# Default: 'development'
# Production builds automatically set this to 'production'
# NODE_ENV=production

# Analytics (optional)
# Uncomment and add your tracking IDs if using analytics
# VITE_GA_TRACKING_ID=G-XXXXXXXXXX
# VITE_PLAUSIBLE_DOMAIN=yourdomain.com

# Feature Flags (optional)
# Enable/disable optional features
# VITE_ENABLE_BUDGET_GOALS=true
# VITE_ENABLE_ANALYTICS=false

# API Configuration (if adding backend in future)
# VITE_API_URL=https://api.example.com
# VITE_API_KEY=your-api-key

# Note: All VITE_ prefixed variables are exposed to the client
# Never put sensitive information in VITE_ variables
